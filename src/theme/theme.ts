import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: ["Oswald", "cursive"].join(","),
    fontWeightLight: 300,
  },
  // palette: {
  //   type: "dark",
  // },
});
